---
description: OOTD-Agent 开发工作流。有新需求、修 bug、改 prompt、架构调整时始终遵循此文件。
globs:
alwaysApply: true
---

# OOTD-Agent 开发工作流

## 项目背景

OOTD-Agent 是一个多 Agent AI 穿搭助手，核心链路：

```
用户消息 → Gatekeeper（意图路由）→ RAG（衣橱检索）→ Stylist（方案生成）
         → Copywriter（文案流式输出）+ VisualDirector（效果图生成）
```

**关键约束**：LLM 行为不稳定，prompt 改动影响全局，任何改动必须有验证闭环。

---

## 工作流四阶段

### 阶段 1：定义（讨论前，切 Plan 模式）

> **不要直接开口让 AI 写代码。**

1. 提供触发此需求的**真实证据**：日志片段、用户反馈截图、`log/*.jsonl` 中的具体记录
2. 在 Plan 模式中描述：
   - 现在的问题行为（Before）
   - 期望的行为（After）
   - 怀疑涉及哪一层（prompt / schema / 路由逻辑 / 服务端）
3. AI 负责确认：改哪层、不改哪层、上下游影响

**产出**：方案文档（可直接写在对话里，复杂需求存入 `design/features/YYYY-MM-DD-xxx.md`）

---

### 阶段 2：规格化（动手前必须完成）

在方案文档中明确**验收标准**，格式：

```markdown
## 验收标准

### 对话场景
- [ ] 用户说「有没有白裙子」→ request_type=wardrobe_pairing, wardrobe_search_query="白色裙子"
- [ ] 用户说「裙子呢」（上轮已问过白裙子）→ 不进入 clarify 死循环

### 单测
- [ ] resolveWardrobeAnchor 颜色冲突时返回 color_mismatch
- [ ] coerceWardrobeBrowseIntent 识别浏览意图并升级为 wardrobe_pairing

### 日志指标
- [ ] gatekeeper-audit.jsonl 中 clarify 比例下降
```

> **没有验收标准不允许进入实施阶段。**

---

### 阶段 3：实施（切 Agent 模式）

#### Prompt / Schema 改动

```
改 schema 字段描述 → 改 prompt 说明 → 改 typeDefs → 跑 tsc → 真实对话验证
```

- schema 字段描述写**原则**，不写死举例（举例会被 LLM 字面照抄，导致 context 不符时出错）
- 每次只改一个 Agent 的 prompt，不混改
- 改完在终端确认 LLM 实际输出：`cat log/gatekeeper-audit.jsonl | tail -5`

#### 纯逻辑改动

```
写单测（先写 failing test）→ 写实现 → 跑通 → commit
```

- 纯函数逻辑（finalize 路由、颜色过滤、schema 规范化）必须有对应单测
- 跑测试命令：`pnpm test` 或 `pnpm exec tsx --test tests/unit/rag/queryColorMatch.test.ts`

#### Prompt 改动

```
改 prompt/schema → pnpm test:prompts → 确认核心场景未回归 → commit
```

- Prompt 回归测试文件在 `tests/prompts/*.prompt.test.ts`
- 调真实 LLM（不 mock），断言关键结构字段（request_type、wardrobe_search_query 等）
- 新增场景时先在文件里加 fixture，跑 failing → 改 prompt → 跑通

#### 改动边界原则

- 每次 commit 只包含一个独立改动，不堆大 commit
- 改了 Agent A，主动检查上下游 Agent B/C 是否需要同步

---

### 阶段 4：验证（声称完成前必须执行）

> **没有输出证据的"完成"叫自嗨。**

按顺序执行：

```bash
# 1. 类型检查
pnpm exec tsc --noEmit

# 2. 纯逻辑单测（快，< 3s，每次必跑）
pnpm test

# 3. Prompt 回归测试（慢，调真实 LLM，改 prompt 时跑）
pnpm test:prompts

# 4. 日志确认（真实对话后）
cat log/gatekeeper-audit.jsonl | tail -3
cat log/visual-audit.jsonl | tail -3
```

验证后在回复中贴出关键输出证据，例如：
```
✅ tsc: 0 errors
✅ tests: 103 pass, 0 fail
✅ gatekeeper-audit: request_type=wardrobe_pairing, wardrobe_search_query="白色裙子"
```

---

## 项目可观测性

开发期间，这些日志是最快的调试手段：

| 日志文件 | 用途 |
|---------|------|
| `log/gatekeeper-audit.jsonl` | Gatekeeper 意图判定结果，看 request_type 分布 |
| `log/rag-search.jsonl` | 衣橱检索命中率、相似度分布 |
| `log/visual-audit.jsonl` | 效果图质量，`approved: false` 说明画错了 |
| `log/copywriter-audit.jsonl` | 文案生成质量 |

终端实时日志前缀对应模块：
```
[GATEKEEPER_AGENT]  Gatekeeper LLM 原始输出
[GATEKEEPER]        finalize 路由决策
[RAG_HANDLER]       RAG 检索过程
[STYLIST_AGENT]     Stylist 生成
[WARDROBE_RESOLVER] 衣橱单品解析（含颜色过滤）
[VISUAL_DIRECTOR]   效果图生成与审核
```

---

## Agent 改动速查

| 想改什么 | 改哪里 |
|---------|-------|
| Gatekeeper 意图判定逻辑 | `server/agents/gatekeeper/prompts.ts` + `schema.ts` |
| 意图路由（clarify/pairing/etc） | `server/agents/intent/finalize.ts` |
| 衣橱检索 query 生成 | Gatekeeper `schema.ts` 中 `wardrobe_search_query` 字段描述 |
| 衣橱检索颜色过滤 | `server/utils/queryColorMatch.ts` |
| RAG XML 格式 / 索引引用 | `server/agents/rag/search.ts` |
| Stylist 输出 schema | `server/agents/stylist/schema.ts` + `prompts.ts` |
| 效果图 prompt 构建 | `server/agents/visual/imageGen.ts` → `buildImagePrompt` |
| 图片生成模型参数 | `server/config/models.ts` + `imageGen.ts` → `IMAGE_GEN_CONFIG` |

---

## 三条红线

🚫 **红线一：闭环意识** — 说完成之前，必须贴出验证输出。没有证据 = 没有完成。

🚫 **红线二：事实驱动** — 说「可能是 X 问题」之前，先用日志或工具验证。猜测不是诊断。

🚫 **红线三：诊断先行** — 改代码前先输出：
```
[DIAGNOSIS] 问题是___；证据是___（来自哪条日志/哪行报错）；下一步改___。
```

---

## 常用命令速查

```bash
pnpm dev                    # 启动开发服务器
pnpm test                   # 纯逻辑单测（< 3s）
pnpm test:prompts           # Prompt 回归测试（调真实 LLM，改 prompt 时跑）
pnpm lint                   # ESLint + tsc
pnpm db:data                # 打开 Prisma Studio 查看数据库

# 快速查日志
tail -f log/gatekeeper-audit.jsonl
tail -f log/visual-audit.jsonl

# 跑单个测试文件
pnpm exec tsx --test tests/unit/rag/queryColorMatch.test.ts
pnpm exec tsx --test tests/unit/intent/intentTypes.test.ts
pnpm exec tsx --test tests/prompts/gatekeeper.prompt.test.ts
```
